// JSONbin.io Guestbook Configuration
// 
// TO MAKE THE GUESTBOOK WORK ONLINE:
// 1. Go to https://jsonbin.io/ 
// 2. Click "Try JSONBin for Free" (no signup needed for testing!)
// 3. Click "Create Bin"
// 4. Replace the JSON with: {"entries": []}
// 5. Copy the Bin ID from the URL (the long string after /bins/)
// 6. Copy your API key (shown on the page)
// 7. Replace the values below

// For now, this will work locally (messages saved in browser only)
const USE_LOCAL_STORAGE = true; // Change to false after setting up JSONbin

// JSONbin.io API Key (get from https://jsonbin.io after sign up)
const JSONBIN_API_KEY = 'YOUR_API_KEY_HERE';

// Guestbook Bin URL (get after creating a bin at https://jsonbin.io)
const GUESTBOOK_BIN_URL = 'https://api.jsonbin.io/v3/b/YOUR_BIN_ID_HERE';
